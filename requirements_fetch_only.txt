# Minimal requirements for fetch operation only
# Core dependencies for job fetching
requests>=2.28.0
pinecone>=5.0.0
supabase>=2.0.0
python-dotenv>=1.0.0
python-dateutil>=2.8.0
pytz>=2023.3

# Required for async Groq API client
aiohttp>=3.8.0

# CLI formatting (lightweight, needed for command line interface)
rich>=12.0.0
